#Существует список [‘яблоки’, ‘апельсины’, ‘бананы’]. Добавить элементы ‘манго’ и ‘виноград’. 
#Удалить из списка элемент ‘апельсины’. Вывести итоговый список и его длину на экран.

a = input().split()
a.remove(a[1])
print(a, "длина списка =", len(a))

#сделала ввод списка через командную строку
