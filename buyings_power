#Написать программу “Управление покупками”. 
#Добавлять в список покупок товары, пока пользователь не введет “стоп”. 
#В конце выполнения программа должна вывести каждый товар с новой строки.
l = input()
lf = list()
while l != "стоп":
    lf.append(l)
    print(lf)
i = 0
while i < len(lf):
    print(lf[i])
    i += 1
